{
  "categories": [
    "Breadth-first Search",
    "Graph"
  ],
  "diff": "Medium",
  "title": "310. Minimum Height Trees",
  "question": "<div><p>For an undirected graph with tree characteristics, we can choose any node as the root. The result graph is then a rooted tree. Among all possible rooted trees, those with minimum height are called minimum height trees (MHTs). Given such a graph, write a function to find all the MHTs and return a list of their root labels.</p>\n\n<p><b>Format</b><br>\nThe graph contains <code>n</code> nodes which are labeled from <code>0</code> to <code>n - 1</code>. You will be given the number <code>n</code> and a list of undirected <code>edges</code> (each edge is a pair of labels).</p>\n\n<p>You can assume that no duplicate edges will appear in <code>edges</code>. Since all edges are undirected, <code>[0, 1]</code> is the same as <code>[1, 0]</code> and thus will not appear together in <code>edges</code>.</p>\n\n<p><b>Example 1 :</b></p>\n\n<pre><strong>Input:</strong> <code>n = 4</code>, <code>edges = [[1, 0], [1, 2], [1, 3]]</code>\n\n        0\n        |\n        1\n       / \\\n      2   3 \n\n<strong>Output:</strong> <code>[1]</code>\n</pre>\n\n<p><b>Example 2 :</b></p>\n\n<pre><strong>Input:</strong> <code>n = 6</code>, <code>edges = [[0, 3], [1, 3], [2, 3], [4, 3], [5, 4]]</code>\n\n     0  1  2\n      \\ | /\n        3\n        |\n        4\n        |\n        5 \n\n<strong>Output:</strong> <code>[3, 4]</code></pre>\n\n<p><b>Note</b>:</p>\n\n<ul>\n\t<li>According to the <a href=\"https://en.wikipedia.org/wiki/Tree_(graph_theory)\" target=\"_blank\">definition of tree on Wikipedia</a>: “a tree is an undirected graph in which any two vertices are connected by <i>exactly</i> one path. In other words, any connected graph without simple cycles is a tree.”</li>\n\t<li>The height of a rooted tree is the number of edges on the longest downward path between the root and a leaf.</li>\n</ul>\n</div>",
  "url": "https://leetcode.com/problems/minimum-height-trees/",
  "companies": {},
  "code": "class Solution(object):\\u000A    \\u000A    # BFS from leave nodes\\u000A    def findMinHeightTrees(self, n, edges):\\u000A        if n \\u003D\\u003D 1: return [0]\\u000A        graph \\u003D [set() for _ in range(n)]\\u000A        for a, b in edges:\\u000A            graph[a].add(b)\\u000A            graph[b].add(a)\\u000A        # leaf nodes\\u000A        bfs \\u003D [node for node, adjs in enumerate(graph) if len(adjs) \\u003D\\u003D 1]\\u000A        while n \\u003E 2:\\u000A            n \\u002D\\u003D len(bfs)\\u000A            new_bfs \\u003D []\\u000A            for node in bfs:\\u000A                parent \\u003D graph[node].pop()\\u000A                graph[parent].remove(node)\\u000A                if len(graph[parent]) \\u003D\\u003D 1: new_bfs.append(parent)\\u000A            bfs \\u003D new_bfs\\u000A        return bfs\\u000A                \\u000A\\u000A    # Cutting leaf nodes until there\\u0027s 2 or less\\u000A    def __findMinHeightTrees(self, n, edges):\\u000A        graph \\u003D {i:set() for i in xrange(n)}\\u000A        for a, b in edges:\\u000A            graph[a].add(b)\\u000A            graph[b].add(a)\\u000A        # iteratively remove leaves until 1/2 nodes left\\u000A        visited \\u003D set()\\u000A        while n \\u002D len(visited) \\u003E 2:\\u000A            leaves \\u003D [node for node, adjs in graph.iteritems() if len(adjs) \\u003D\\u003D 1]\\u000A            for leave in leaves:\\u000A                for adj in graph[leave]:\\u000A                    if leave in graph[adj]: \\u000A                        graph[adj].remove(leave)\\u000A                del graph[leave]\\u000A            visited.update(set(leaves))\\u000A        return list(set(range(n)) \\u002D visited)"
}